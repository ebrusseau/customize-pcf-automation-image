---
resource_types:
- name: pivnet
  type: docker-image
  source:
    repository: pivotalcf/pivnet-resource
    tag: latest-final

resources:
- name: platform-automation-resource
  type: pivnet
  source:
    api_token: {{pivnet-token}}
    product_slug: platform-automation

- name: customizations
  type: git
  source:
    uri: https://github.com/ebrusseau/customize-pcf-automation-image.git

- name: custom-image
  type: docker-image
  source:
    repository: ebrusseau/custom-pcf-automation
    username: {{docker-username}}
    password: {{docker-password}}

jobs:
- name: customize-image
  plan:
    - get: platform-automation-image
      resource: platform-automation-resource
      params:
        globs: [ "platform-automation-image*.tgz" ]
        unpack: true
    - get: customizations
    - put: custom-image
      params:
        load_file: platform-automation-image
        load_repository: platform-image
        load_tag: latest
        build: customizations
